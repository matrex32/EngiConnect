import React from 'react';
import ReactDOM from 'react-dom/client';

import SearchedUserProfileContainer from './searched user/SearchedUserContainer.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SearchedUserProfileContainer/>
  </React.StrictMode>
);